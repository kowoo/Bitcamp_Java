메서드에 대해서 실습합니다.
 
매개변수 : 메서드를 실행할 때, 외부의 값이 필요하면 선언하는 변수, 
                 사용범위는 지역변수와 같다. ex) public student (Score sc){} 에서 (Score sc) 부분(스코어타입의 sc라는 변수)
반환형  : 메서드를 실행하고나서 어떤 결과값을 얻고자 할 때 결과값의 데이터 타입
반환값  : return 문장뒤에 작성된다. 메서드가 호출된 자리에 값이 반환 됨 
       return 메서드의 종료를 의미한다.

Q1. 메서드는 무엇인가?? 
A. 메서드는 문장들의 모음
 
Q2. 메서드는 언제 사용하나??
A. 자주사용되는 문장들을 모아서 필요할 때마다 불러서 사용하기 위해 선언

Q3. 메서드의 모양
A.	수식자 반환형 이름(매개변수){
		문장;
		...
		return 반환값;
	}
	   
Q4. 메서드 사용
A. 메서드를 선언한 뒤, 사용하기 위해서는 호출해야한다.
       호출은 메서드이름(); 형태로 한다.
       매개변수를 선언했다면, 매개변수에 맞는 데이터를 인자로 넘겨주어야 한다.
   ex)
	//메서드 선언
	public void method1(int num,double num2){ // 매개변수 명은 중복불가(타입이 다르더라도 같으면 안된다.)
	문장
	}
	//메서드 호출
	method1(5,2.7);





